{
  "name": "metrics-health-service",
  "version": "1.0.0",
  "description": "Simple Node.js service exposing /health and /metrics for Prometheus",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "lint": "node -e \"console.log('Lint: skipped (no linter configured)')\"",
    "test": "node -e \"require('http').get('http://localhost:3000/health', (r)=>{process.exit(r.statusCode===200?0:1)}).on('error',()=>process.exit(1))\""
  },
  "author": "blackboxai",
  "license": "MIT",
  "dependencies": {
    "express": "^4.19.2",
    "prom-client": "^15.1.3"
  }
}
